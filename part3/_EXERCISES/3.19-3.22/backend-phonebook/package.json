{
  "name": "backend-phonebook",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../frontend-phonebook && npm run build && cp -r dist ../backend-phonebook",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push",
    "switch" : "cd ../backend-notes/ && npm run dev"
  },
  "author": "Sean Kennedy",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "mongodb": "^6.18.0",
    "mongoose": "^8.18.0",
    "morgan": "^1.10.1",
    "nodemon": "^3.1.10"
  }
}
